import styled from 'styled-components';

export const Container = styled.div`
    background: #0192d5;
    width: 100vw;
    height: 100px;
    position: absolute;
    z-index: 1;
    display: flex;
    flex-direction: row;
    justfy-content: center;
    align-items: center;
    color: #ffffff;
    font-weight: bolder;
    font-size: 1rem;

    .logoPlace{
        margin-left: 15rem;
        margin-top: 0.25rem;
        width: 50%;
    }
`
